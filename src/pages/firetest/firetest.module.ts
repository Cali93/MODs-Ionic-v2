import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FiretestPage } from './firetest';

@NgModule({
  declarations: [
    FiretestPage,
  ],
  imports: [
    IonicPageModule.forChild(FiretestPage),
  ],
})
export class FiretestPageModule {}
